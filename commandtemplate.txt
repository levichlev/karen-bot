(All commands go into the cmds folder)
(If making Rich Embed, put this ".setFooter('Author - YourDiscordName#1010', 'DirectImgurLinkToProfileImage')")



const app = require("../bot.js");
let client = app.client;
let Discord = app.Discord;
let config = app.config;
        
function run(msg, args) {
     // code here will run
    
}
exports.run = run;